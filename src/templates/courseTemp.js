import * as React from "react"
import Layout from "../components/layout"

const CourseTemp = () => (
  <Layout>
    <card>
    <p>
      check if this works
    </p>
  </card>
  </Layout>
  
)

export default CourseTemp